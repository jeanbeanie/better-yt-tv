# loaded by vite build automatically (production mode), no reliance on
# Railway passing build-time env vars through, this guarantees a relative
# API base regardless of platform quirks
VITE_API_BASE_URL=
